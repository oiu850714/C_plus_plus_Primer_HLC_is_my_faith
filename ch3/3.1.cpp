#include "3.1.header.h"
int cout = 100;

int main() {}

// the original problem is too tedious so I don't write it, instead I write a
// demo for using declaration coliision when use using declaration on header we
// included