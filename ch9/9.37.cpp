// array cannot reallocate, its capacity == its size
// list don't store element continuously, so there is no capacity concept like
// "how many elements it can hold before more space must be allocated"