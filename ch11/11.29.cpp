// upper_bound and lower_bound will both return iter that can be used to insert
// given key that will not disrupt elements' order;
// equal_range will return {upper_bound(key), lower_bound(key)}, both are the
// same.