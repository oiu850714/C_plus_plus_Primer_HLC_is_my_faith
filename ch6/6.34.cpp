// although this version of factorial will fail when we pass negative value to
// it, but I think we should use more robust error handling technique instead of
// just let the function recurse until 1