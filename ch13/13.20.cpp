// All copy-control are left synthesized.
// So all member are copied, assigned, destructed by default.
// That is, shared_ptr and map in TextQuery, and string and shared_ptr in
// QueryResult, are copied and so on by default.