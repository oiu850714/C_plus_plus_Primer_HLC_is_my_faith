// using declaration introduces one member in a namespace into current scope;

// using directive injects all members in namespace into scope that contains
// both the namespace itself and the using directive;