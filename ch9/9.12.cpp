// SEE Primer p.334~335

// 只接受一個 container 當參數的 constructor，container 的 type
// 要完全一樣，完全一樣是指 container 本身跟 element type 都要一樣 接受兩個

// iterator 當參數的 constructor，container 本身不需要一樣，並且 iterator 指向的
// type 只要可以轉換為 constructor 對應的 object 的 type 就可以了
