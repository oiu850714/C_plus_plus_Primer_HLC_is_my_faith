// the iterator range [first, last) meet requirments
// if last they refer to elements of, or one past the end of, the same container
// and it is possible to reach end by repeatedly incrementing begin
// SEE C++ Primer p.331