// if elem1 == elem2, no elements are erased, and erase return elem1

// if elem2 is off-the-end iterator, then erase will remove elements in [elem1,
// off-the-end-iteraotr)

// if both elem1 and elem2 are off-the-end iterator, then nothing will happene